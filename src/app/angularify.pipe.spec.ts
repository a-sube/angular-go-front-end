import { AngularifyPipe } from './angularify.pipe';

describe('AngularifyPipe', () => {
  it('create an instance', () => {
    const pipe = new AngularifyPipe();
    expect(pipe).toBeTruthy();
  });
});
